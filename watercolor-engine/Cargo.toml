[package]
name = "watercolor-engine"
version = "0.1.0"
edition = "2021"
description = "Watercolor simulation physics engine in Rust/WASM"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"

[profile.release]
opt-level = 3
lto = true
